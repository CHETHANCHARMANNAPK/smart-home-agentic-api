# Glass Data Submission Checklist

- [ ] Create a clean virtual environment.
- [ ] Install `requirements.txt`.
- [ ] Run `pytest -q` and confirm all tests pass locally.
- [ ] Run `uvicorn app.main:app --reload`.
- [ ] Open `/docs` and perform the three demo scenarios in `evaluation-notes.md`.
- [ ] Inspect the architecture diagram in `docs/architecture.svg`.
- [ ] Confirm `.env` or API keys are NOT included in the ZIP.
- [ ] Zip the project root, excluding `.venv`, `__pycache__`, `.pytest_cache`, and secrets.
- [ ] Name the ZIP `Chethan_Charmanna_PK_GlassData_SmartHome_Agent.zip`.
- [ ] Send the ZIP with a short professional submission email.
