# Default is deterministic and requires no API key.
PLANNER_PROVIDER=mock

# Optional OpenAI-compatible LLM planner.
# Example base URL should point to a provider exposing /chat/completions.
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
